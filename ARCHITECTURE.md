# Architecture

The repository separates the Next.js presentation layer (`app/`, `components/`, `lib/`) from the Python research and domain layer (`src/gridmatch/`) and the HTTP boundary (`api/`). Data and model artifacts have dedicated versionable locations. Detailed architecture decisions belong to later implementation phases.
