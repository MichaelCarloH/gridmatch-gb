# Copy to .env.local when later phases introduce credentials.
# All public-data clients must document keys and rate limits in DATA_SOURCES.md.
GRIDMATCH_ENV=development
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
