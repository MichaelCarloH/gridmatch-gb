# Cached public fixtures

Files in this directory are compact snapshots derived directly from successful calls to the official source URLs recorded in their companion manifests. They exist only to keep the Phase 3 demonstration reproducible when a source is unavailable. Their records retain `data_origin=public`, while manifests and fetch results state `retrieval_mode=fixture`; they are never represented as live retrievals or simulated customer data.
