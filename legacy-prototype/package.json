{
  "name": "gridmatch-gb",
  "version": "0.1.0",
  "private": true,
  "description": "Public-data and synthetic-meter-data clean energy intelligence prototype.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "generate:data": "python scripts/generate_synthetic_data.py"
  },
  "engines": { "node": ">=18" }
}
